# ── Server ──
HOST=0.0.0.0
PORT=8000
DEBUG=false

# ── Security ──
# Shared secret for bot ↔ POS communication (generate a strong random string)
API_KEY=your-secret-api-key-change-me

# Comma-separated Telegram bot tokens (for initData validation in Mini App)
BOT_TOKENS=123456:ABC-staff-token,789012:DEF-tourist-token

# ── Database (PostgreSQL) ──
DATABASE_URL=postgresql+asyncpg://pos_user:pos_password@localhost:5432/pos_db

# ── Google Sheets ──
STORE_SPREADSHEET_ID=your-spreadsheet-id-here
GOOGLE_SA_PATH=google_service_account/

# ── NSPK Payment Gateway ──
NSPK_SLUG=perm_52edadbda3b3c235bad91e9e76025d4c
NSPK_POINT_ID=261

# ── Pier Identity ──
PIER_NAME=Yamu

# ── CORS ──
# Use "*" for development, or comma-separated origins for production
CORS_ORIGINS=*
